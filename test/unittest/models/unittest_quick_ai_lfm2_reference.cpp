// SPDX-License-Identifier: Apache-2.0
/**
 * Copyright (C) 2026 Samsung Electronics Co., Ltd. All Rights Reserved.
 *
 * @file   unittest_causallm_lfm2_reference.cpp
 * @date   19 June 2026
 * @brief  Differential (cross-validation) tests for the LFM2 CausalLM model.
 *
 * Compares nntrainer's LFM2 output against golden fixtures generated by
 * HuggingFace transformers (generate_lfm2_reference.py). The generic checks
 * live in causallm_test_utils; this file only binds the LFM2 adapter and the
 * fixture name. Tests skip automatically when fixtures or the nntr_quantize
 * binary are absent.
 *
 * @see    https://github.com/nntrainer/nntrainer
 * @author Jungwon Lee <jungone.lee@samsung.com>
 * @bug    No known bugs except for NYI items
 */

#include <quick_ai_test_utils.h>

#include <gtest/gtest.h>

#include <lfm2_causallm.h>

#include <memory>

namespace {

/**
 * @brief Differential model descriptor for the tiny LFM2 fixture
 */
quick_ai_test::DifferentialModel lfm2Model() {
  return {
    "lfm2_tiny",
    [](quick_ai::json &cfg, quick_ai::json &gen_cfg, quick_ai::json &nntr_cfg) {
      return std::make_unique<
        quick_ai_test::CausalLMTestAdapter<quick_ai::Lfm2CausalLM>>(
        cfg, gen_cfg, nntr_cfg);
    },
  };
}

/**
 * @brief FP32 prefill logits and greedy tokens match the HF reference
 */
TEST(Lfm2DifferentialTest, FP32MatchesHFReference) {
  quick_ai_test::runFp32DifferentialChecks(lfm2Model());
}

/**
 * @brief Q4_0 quantized logits stay within tolerance of the FP32 reference
 */
TEST(Lfm2DifferentialTest, Q40CloseToFP32Reference) {
  quick_ai_test::runQ40DifferentialChecks(lfm2Model());
}

} // namespace
